name: Build Executables

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:  # Allows manual trigger

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: Build Windows executable
      run: |
        pyinstaller --onefile --windowed --name "StardewModLoader" main.py
    
    - name: Upload Windows executable
      uses: actions/upload-artifact@v4
      with:
        name: StardewModLoader-Windows
        path: dist/StardewModLoader.exe

  build-macos:
    runs-on: macos-13  # Use Intel-based runner for better compatibility
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
    
    - name: Build macOS executable
      run: |
        # Build for x86_64 (will work on Intel and Apple Silicon via Rosetta 2)
        pyinstaller --onefile --windowed --name "StardewModLoader" main.py
    
    - name: Create DMG (optional)
      run: |
        # Create a simple distributable folder
        mkdir -p dist-macos
        cp -r dist/StardewModLoader.app dist-macos/
    
    - name: Upload macOS executable
      uses: actions/upload-artifact@v4
      with:
        name: StardewModLoader-macOS
        path: dist/StardewModLoader.app
